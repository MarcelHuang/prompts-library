# Prompt: Clean Up and Reformat a Markdown File

## Instructions

I'm attaching a markdown file that needs to be cleaned up and reorganized into a properly formatted `.md` file.

Please do the following:

1. **Preserve all original content** — do not remove, rewrite, or reinterpret any logic, instructions, or copy. The goal is structural cleanup, not content editing.
2. **Add a clear document structure** with a title, overview/purpose section, and logical section flow using proper heading hierarchy (`#`, `##`, `###`)
3. **Fix formatting issues**, including:
   - Convert plain text lists into proper markdown lists or checkboxes where appropriate
   - Break up walls of text or monolithic code blocks into labeled sections
   - Apply consistent formatting for repeated structures (e.g. if a pattern repeats, give it a consistent template)
   - Add horizontal rules (`---`) to separate major sections for readability
4. **Improve scannability** — use dividers, whitespace, and parallel structure so the document is easy to navigate at a glance
5. **Output a single `.md` file** ready to download

Do not add new content, commentary, or your own interpretation. Just clean structure and consistent formatting.
